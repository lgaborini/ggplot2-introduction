remark.macros.package_cran = function (name) {
  return '📦 <a href="https://cran.r-project.org/package=' + name + '"><code class="link">' + name + '</code></a>';
};

remark.macros.read_the_docs = function (name) {
  return '📖 documentation !';
};